
function tabs(){const tabs=document.querySelectorAll('[data-tab]');const onRemove=(block,nav)=>{block.style.opacity='0';block.style.overflow='hidden';block.style.height='0px';nav.classList.remove('_active');};const onAdd=(block,nav)=>{block.style.opacity='1';block.style.overflow='visible';block.style.height='auto';nav.classList.add('_active');};tabs.forEach((tab)=>{const navigations=tab.querySelectorAll('[data-tab-button]');const screens=tab.querySelectorAll('[data-tab-name]');navigations.forEach((button)=>{button.addEventListener('click',()=>{const targetID=button.getAttribute('data-tab-button');const formatID=targetID.toString().toLowerCase();screens.forEach((screen)=>{const screenID=screen.getAttribute('data-tab-name');const formatScreenID=screenID.toString().toLowerCase();if(formatID===formatScreenID){const items=tab.querySelectorAll('[data-tab-name]');items.forEach((item)=>{navigations.forEach((nav)=>{onRemove(item,nav);});});onAdd(screen,button);}});});});});}
export default tabs;